# LTX.CLI

Command-line interface for the LTX SDK generated with AutoSDK.

## Installation

```bash
dotnet tool install --global LTX.CLI --prerelease
```

## Usage

```bash
ltx --help
ltx api --help
```